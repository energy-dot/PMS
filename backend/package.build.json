{
  "name": "pms-backend",
  "version": "1.0.0",
  "description": "Partner Management System Backend",
  "scripts": {
    "build": "nest build -p tsconfig.build.json",
    "start": "node dist/main",
    "start:dev": "nest start --watch",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch --passWithNoTests",
    "test:cov": "jest --coverage --passWithNoTests"
  },
  "comment": "このファイルは既存のpackage.jsonに以下の修正を加えてください: 1. buildスクリプトを 'nest build -p tsconfig.build.json' に変更、2. テストコマンドに '--passWithNoTests' フラグを追加"
}
