{
  "name": "pms-shared-types",
  "version": "1.0.0",
  "description": "共有型定義パッケージ",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepare": "npm run clean && npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^5.0.1",
    "typescript": "^5.0.4"
  },
  "files": [
    "dist"
  ]
}
